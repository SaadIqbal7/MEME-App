<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
	android:layout_width="match_parent" android:layout_height="wrap_content"
	android:padding="16dp"
	android:background="@drawable/border_list_item_style">

	<ImageView
		android:id="@+id/template_image"
		android:layout_width="70dp"
		android:layout_height="70dp"
		android:src="@drawable/pooh_template"
		android:background="@drawable/image_view_border"/>

	<TextView
		android:id="@+id/template_name_text_view"
		android:layout_width="wrap_content"
		android:layout_height="wrap_content"
		android:text="Winnie the pooh"
		android:layout_toEndOf="@id/template_image"
		android:layout_toStartOf="@id/asd"
		android:layout_centerVertical="true"
		android:layout_marginStart="16dp"
		android:textColor="@color/primary_dark"
		android:fontFamily="@font/segoe_ui"
		android:textSize="16sp"/>

	<ImageView
		android:id="@+id/asd"
		android:layout_width="24dp"
		android:layout_height="24dp"
		android:layout_alignParentEnd="true"
		android:src="@drawable/ic_keyboard_arrow_right_black_24dp"
		android:layout_centerVertical="true"/>
</RelativeLayout>
